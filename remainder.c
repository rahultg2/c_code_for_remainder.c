#include<stdio.h>
#include<math.h>
int main(){
	int a, b;
	printf("Enter two numbers: "); //For take input
	scanf("%d %d", &a, &b);
	printf("%d", a%b); // Reminder action perform here
	return 0;
}
